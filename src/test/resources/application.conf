test-value: 100500
inject-modules: [TestInjector]
test-env: {
  int-value: 100
  int-value~prod: 20
  int-value~qa: 200

  object-value: {
    name: "default"
    removed: "some"
  }

  object-value~prod: {
    name: "production"
  }

  object-value~qa: {
    name: "qa"
  }

  resolve: ${test-env.int-value}
}
